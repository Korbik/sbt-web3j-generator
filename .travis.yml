language: scala
scala:
    - 2.12.4
jdk:
    - oraclejdk8
cache:
    directories:
        - "$HOME/.m2/repository"
        - "$HOME/.sbt"
        - "$HOME/.ivy2"
jobs:
    include:
        - stage: deploy
          install: skip # bundle install is not required
          script: "sbt ++$TRAVIS_SCALA_VERSION publish"